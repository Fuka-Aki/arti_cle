<div class="container-fluid">
  <div class="jumbotron">
    <h1>Arti<span class="glyphicon glyphicon-cd" aria-hidden="true"></span>Cle</h1>
    <p>-Artistの輪を広げよう-</p>
  </div>
</div>

<%# アーティストランキングの表示 %>
<%= render 'shared/ranking', all_ranks: @all_ranks %>

<%# 投稿したアーティストを表示 %>
<div class="pickup-artist">
  <div class="page-header">Pick Up Artist</div>
  <div class="row">
  <% @rooms.each do |room| %>
    <%= render 'shared/artist_list', room: room %>
  <% end %>
  </div>
</div>

<%# ページネーション %>
<%= paginate @rooms %>