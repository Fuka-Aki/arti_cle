<nav class="navbar-dark">
  <div class="container-fluid">
    <div class="navbar-header">
    <% if user_signed_in? %>
      <a class="navbar-brand" href="/rooms">Arti<span class="glyphicon glyphicon-cd" aria-hidden="true"></span>Cle</a>
    <% else %>
      <a class="navbar-brand" href="/">Arti<span class="glyphicon glyphicon-cd" aria-hidden="true"></span>Cle</a>
    <% end %>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if user_signed_in? %> 
      <ul class="nav navbar-nav">
        <li><%= link_to 'お問い合わせ', inquiries_path, class: "inquiry" %></li>
        <li><%= link_to 'New Post', new_room_path, class: "NewPost" %></li>
        <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="input-sm form-control" placeholder="Search">
        </div>
        </form>
      </ul>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %> 
        <li><%= link_to current_user.nickname, user_path(current_user.id), class: "UserName" %></li>
        <li><%= link_to 'LogOut', destroy_user_session_path, method: :delete, class: "LogOut" %></li>
      <% else %>
        <li><%= link_to 'Log In', new_user_session_path, class: "LogIn" %></li>
        <li><%= link_to 'Sign Up', new_user_registration_path, class: "SignUp" %></li>
        <li><%= link_to 'Guest User', users_guest_sign_in_path, method: :post, class:"GuestUser" %></li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>